import React from "react";
import styled from "styled-components";

const SearchBar = () => {
  return <Adiv>SearchBar</Adiv>;
};

export default SearchBar;

const Adiv = styled.div`
  background-color: red;
`;
